# Ежедневник (Daily Diary)

Десктопное приложение для ведения личных записей, разработанное на **Tauri + Vue 3**.

## 🚀 Технологии

- **Frontend:** Vue 3, TypeScript, Pinia, Vue Router, SCSS Modules
- **Backend:** Rust, Tauri 2.x
- **Хранение данных:** JSON файлы (через Tauri FS API)

## 📋 Требования

### Для разработки

- Node.js 18+ и pnpm
- Rust 1.70+
- Системные зависимости (Linux):

```bash
# Ubuntu/Debian
sudo apt install libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev libgdk-pixbuf2.0-dev pkg-config

# Fedora
sudo dnf install webkit2gtk4.1-devel gtk3-devel libayatana-appindicator3-devel librsvg2-devel libsoup3-devel javascriptcore4.1-devel pkg-config

# Arch Linux
sudo pacman -S webkit2gtk-4.1 gtk3 libayatana-appindicator librsvg libsoup3 javascriptcoregtk-4.1 pkgconf
```

## 🛠 Установка и запуск

```bash
# Установка зависимостей
pnpm install

# Запуск в режиме разработки
pnpm tauri dev

# Сборка приложения
pnpm tauri build
```

## 📁 Структура проекта

```
workbook/
├── src/                      # Frontend код
│   ├── components/           # Vue компоненты
│   ├── layouts/              # Layout компоненты
│   ├── views/                # Страницы приложения
│   ├── stores/               # Pinia хранилища
│   ├── types/                # TypeScript типы
│   ├── styles/               # SCSS стили
│   ├── router.ts             # Настройка роутинга
│   ├── main.ts               # Точка входа
│   └── App.vue               # Корневой компонент
├── src-tauri/                # Backend код (Rust)
│   ├── src/
│   │   └── main.rs           # Точка входа Rust
│   ├── capabilities/         # Tauri permissions
│   ├── Cargo.toml            # Rust зависимости
│   └── tauri.conf.json       # Конфигурация Tauri
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## ✨ Функционал

### Реализовано:
- ✅ Просмотр и создание записей по датам
- ✅ Календарь для навигации
- ✅ Категории записей
- ✅ Экспорт/импорт данных (JSON)
- ✅ Тёмная/светлая тема
- ✅ Адаптивный layout

### В разработке:
- ⏳ Поиск и фильтрация записей
- ⏳ Теги для записей
- ⏳ Напоминания
- ⏳ Резервное копирование

## 🎨 Компоненты

### MainLayout
Базовая структура приложения с боковой панелью навигации.

### DiaryView
Основной экран для просмотра и создания записей.

### CalendarView
Календарь с отображением дней, содержащих записи.

### SettingsView
Настройки приложения (тема, уведомления, экспорт/импорт).

## 🔧 Настройка

### Изменение идентификатора приложения

Откройте `src-tauri/tauri.conf.json` и измените `identifier`:

```json
{
  "identifier": "com.yourname.daily-diary"
}
```

## 📝 Лицензия

MIT
